# Implementation Plans

Generated by the improve skill on 2026-07-02 against commit `8790bdb`, with a
deep-audit addendum against commit `e04f37d`. Reconciled in full on 2026-07-12
against `preview` commit `2bba19c`.

The original implementation queue (001-021) is closed. Plans 001-006 and 008-021
are implemented or completed design spikes. Plan 007 is retired because its STOP
condition was triggered and the later AU-only launch decision removed the need
to re-run it now. Keep these files as historical implementation records; new
work must use the next monotonic plan number.

**A new audit batch (plans 022-036) was added on 2026-07-12 against `preview`
commit `123bbd8`** from a fresh `improve` run, and described in its own section
below ("Audit batch 2026-07-12"). Reconciled on 2026-07-14 against `preview`
commit `dabb529`: **022-032 are DONE and merged; 033-036 remain TODO.** All prior
numbering and status is preserved.

## Record conventions

- Numbered `*-report-*.md` files for plans 006-008 are spike deliverables, not
  competing implementation plans. Their duplicate prefix preserves the link to
  the plan that produced them.
- Historical drift checks and unchecked task boxes describe the executor's
  original instructions. The status table below is authoritative after
  reconciliation.
- DONE means the intended outcome is present at current HEAD. REJECTED means
  the plan must not be executed as written.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Stripe webhook: fail loudly, drop placeholder org id, add tests | P1 | M | none | DONE, verified at `2bba19c` |
| 002 | Stop logging full Clerk webhook body (PII) | P1 | S | none | DONE, implemented in `f84fe54`, verified at `2bba19c` |
| 003 | region_not_supported_error for NZ/UK write-back stubs | P2 | S | none | DONE, implemented in `6f6c572`, verified at `2bba19c` |
| 004 | Security response headers + report-only CSP on apps/app | P2 | M | none | DONE, implemented in `c7e045e`, verified at `2bba19c` |
| 005 | Tests for ICS feed route and availability routes | P2 | M | none | DONE, implemented in `dda1776`, verified at `2bba19c` |
| 006 | Pricing surface decision spike (doc-only) | P1 | S | none | DONE, decision implemented by plan 020, verified at `2bba19c` |
| 007 | NZ payroll write-back spike (doc-only) | P2 | M | none | REJECTED, inbound NZ absence triggered STOP and AU-only launch scope superseded the spike |
| 008 | Manager weekly digest design spike (doc-only) | P3 | S | none | DONE, report retained with stale historical citations; implementation not started |
| 009 | Dispatch feed_token_rotated on rotation | P3 | S-M | none | DONE, implemented in `114778d`, verified at `2bba19c` |
| 010 | Provision a default calendar feed for every new organisation | P1 | M | none | DONE, implemented in `e85451a`, verified at `2bba19c` |
| 011 | Provision default public holidays for every new organisation | P1 | M | 010 | DONE, implemented in `c8a0e29`, verified at `2bba19c` |
| 012 | Authorise manual availability mutations by actor and person | P1 | M | none | DONE, verified at `2bba19c` |
| 013 | Repair integration database schema drift before relying on integration tests | P1 | S-M | none | DONE, no migration change required; verified at `2bba19c` |
| 014 | Upgrade high-risk runtime dependencies reported by audit | P1 | M | 013 for full integration verification | DONE, later dependency upgrades retained a clean high-severity audit at `2bba19c` |
| 015 | Align calendar event detail manager scope with calendar range scope | P1 | S | none | DONE, implemented in `374d02d`, verified at `2bba19c` |
| 016 | Align region setup and marketing surfaces with AU-only launch scope | P1 | M | 003 | DONE, implemented in `c4be861`, verified at `2bba19c` |
| 017 | Restrict the Xero OAuth start route to admins and owners | P1 | S | none | DONE, implemented in `79808e5`, verified at `2bba19c` |
| 018 | Revoke the Xero connection on disconnect (DELETE /connections/{id}) | P2 | M | none | DONE, implemented in `11cd418`, verified at `2bba19c` |
| 019 | Harden the Xero token refresh strategy (classify errors, mark stale, lock manual path) | P1 | M | none | DONE, implemented in `9c17067`, landed and verified at `2bba19c` |
| 020 | Replace Clerk pricing table fallback with static catalogue-driven pricing cards | P1 | S | 006 | DONE, implemented in `9aa0bcc`, verified at `2bba19c` |
| 021 | Isolate reconciliation integration fixture IDs | P1 | S | none | DONE, implemented in `040848a`, verified at `2bba19c` |
| 022 | Make `showDeclinedOnApprovals` functional and fix the inverted service default | P1 | S-M | none | DONE, merged to `preview` (`4f3484c`), verified at `dabb529` |
| 023 | Stop the ICS feed cache from invalidating its own key | P1 | S | none | DONE, merged to `preview` (`a402274`), verified at `dabb529` |
| 024 | Remove the orphaned, unauthorised approval-write action | P1 | S | none | DONE, merged to `preview` (`7b71d75`), verified at `dabb529` |
| 025 | Add composite index on `availability_records.approval_status` | P2 | S | none | DONE, merged to `preview` (`f6ce0cf`), verified at `dabb529` |
| 026 | Guard the Stripe subscription mirror against out-of-order webhooks | P2 | S | none | DONE, merged to `preview` (`7bc1491`), verified at `dabb529` — but left `apps/api/.../payments/route.ts` failing `bun run check` (formatting only; see Open defect) |
| 027 | Paginate Xero reads and guard stale-archive against a truncated fetch | P1 | M | none | DONE, executor commit `12efa92`, merged to `preview` as `079fc97`, verified at `dabb529` |
| 028 | Increment feed SEQUENCE when leave dates change | P2 | S-M | none | DONE, merged to `preview` (`530a2cc`), verified at `dabb529` |
| 029 | Stop publishing `notes_internal` into the token-served ICS feed | P2 | S | none | DONE, merged to `preview` (`38818c9`), verified at `dabb529` |
| 030 | Drain `notification_email_queue` and send via Resend | P2 | M | none | DONE, merged to `preview` (`ab1be48`), verified at `dabb529` |
| 031 | Make SSE notifications work across processes (replace in-memory broker) | P2 | L | none | DONE, merged to `preview` (`6dd5d99`), verified at `dabb529` |
| 032 | Stop notification failures rolling back Xero-confirmed state transitions | P2 | M | none | DONE, merged to `preview` (`4a967cb`), verified at `dabb529` |
| 033 | Harden the Xero refresh transaction boundary (CAS on token persist) | P3 | M | none | TODO, no drift at `dabb529` (planned at `123bbd8`; in-scope files untouched) |
| 034 | Build the S-16 Out-of-office analytics route | P3 | M | none | TODO, no drift at `dabb529` (planned at `123bbd8`; in-scope files untouched) |
| 035 | Add CSV export to leave-reports (and out-of-office) analytics | P3 | S-M | 034 (for OOO screen only) | TODO, no drift at `dabb529` (planned at `123bbd8`; in-scope files untouched) |
| 036 | Enable withdraw of approved leave + admin-withdraw-any (spike-first) | P3 | S-M | none (032 landed) | TODO, **refreshed at `dabb529`** after drift; admin-any found already implemented |
| 037 | Run CI on `preview`, and unbreak the formatter so it passes | P1 | S | none | TODO (added by reconcile 2026-07-14, planned at `dabb529`) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

Verification on 2026-07-12: `bun run test` passed all 10 test tasks and
`bun run check` checked 692 files with no fixes. Plan-specific source and test
evidence was spot-checked for every row. Integration and external-service gates
were not rerun unless already recorded in the individual plan.

## Reconciliation 2026-07-14 (against `preview` = `dabb529`)

Plans 022-032 are **all merged into `preview`** and were verified as a *merged
whole* for the first time (each had previously only been verified inside its own
isolated executor worktree). Results at `dabb529`:

- `bun run test` — **10/10 task groups pass** (jobs 32, api 92, app 148, plus
  package suites).
- `bun run typecheck` — **18/18 pass**.
- `bun run check` — **FAILS**. One formatter error, see "Open defect" below.
- Per-plan source evidence re-read against live code for 022-032 (cache key,
  removed action file, migrations, pagination + truncation guard, SEQUENCE
  date-change comparison, post-commit notification dispatch, queue consumer
  registration, Redis SSE transport).

Index corrections made: **029 and 032 were listed TODO but had in fact landed**
and passed their done criteria. 036 was refreshed after drift (see below).
No work was found stranded — every `improve/*` branch is an ancestor of
`preview`.

### Open defect (blocks a clean `bun run check`)

`apps/api/app/webhooks/payments/route.ts` fails Biome's formatter at two call
sites (~lines 117 and 139): the second argument added to `mirrorSubscription(...)`
by plan 026 (`7bc1491`) pushed both calls over the line-width limit and they were
never re-wrapped. It is **purely cosmetic** (formatting only, no lint rule, no
type error) and `bun run fix` resolves it. It is present on both `preview` and the
`improve/026-stripe-ordering-guard` branch. This is the only thing standing
between `preview` and a fully green gate.

### Structural finding: CI never runs on `preview`

`.github/workflows/ci.yml` triggers only on `push`/`pull_request` to **`main`**,
but every plan in this batch develops on and merges to `preview`. Consequently
plans 022-032 landed on `preview` with **zero CI coverage**, which is precisely
how the formatter regression above survived the merge. **Now planned**: plan 037
adds `preview` to the CI triggers and fixes the formatter break first, so the
first `preview` run is green. Execute it before the remaining backlog.

Plans 017–018 added 2026-07-04 from a `plan` request scoping the Xero
authentication functionality ("admins authenticate Xero; available to all users
in the org"). See "Xero authentication: state at 2026-07-04" below for why the
rest of that feature needed no plan.

Plan 019 added 2026-07-04 from a `plan` request scoping the Xero token refresh
strategy. See "Xero token refresh: state at 2026-07-04" below.

## Audit batch 2026-07-12 (plans 022-036)

Generated by the `improve` skill against `preview` commit `123bbd8`. Every
finding was re-read against live code before planning. The batch was reviewed
and corrected on 2026-07-13 (plan-by-plan re-verification against live code;
see the individual plans for corrections). The original execution order ran the
correctness/security fixes 022-024 first, then 027, then 025-026, the
notification pair 030-031, the feeds pair 028-029, and 032. **All of those have
landed on `preview` as of 2026-07-14.**

**Remaining executable work**, in recommended order (all independent of each
other; none is blocked):

1. **037** — Run CI on `preview` + unbreak the formatter. **Do this first.** It is
   the smallest plan in the backlog and it restores the gate whose absence let the
   formatter regression reach `preview` in the first place. `preview` currently
   fails `bun run check`.
2. **034** — Out-of-office analytics route (S-16). Highest user-visible value of
   the rest; the service layer already exists, only the route is missing.
3. **035** — CSV export for analytics. The leave-reports half stands alone; the
   out-of-office export button needs 034 first.
4. **036** — Withdraw approved leave. Refreshed 2026-07-14; smaller than
   originally scoped (admin-withdraw-any already works). Still spike-first: the
   Xero approved-leave reversal semantics remain the open unknown.
5. **033** — Xero refresh CAS hardening. Lowest priority; a P3 hardening pass on
   already-landed plan 019.

Refer to the authoritative "Execution order & status" table above for status.

### Batch dependency notes

- **All plans in this batch develop on `preview`, not `main`.** Create each
  feature branch from `preview` and merge back into `preview` (consistent with how
  plans 001-021 landed). `main` is the eventual release branch; do not branch
  development plans off it or target it. Because plans land on `preview`
  sequentially, an earlier plan's changes may legitimately appear in a later
  plan's drift-check diff.
- **022, 023, 024** are independent, self-contained correctness/security fixes;
  land them first. Note 022's original diagnosis was corrected on review: the
  inverted service default is unreachable from every live caller (the approvals
  page and dashboard always pass explicit filters), so the real defect is that
  the setting does nothing; the corrected plan also aligns the approvals page
  default.
- **027** is the highest-impact bug (recurring tenant-wide leave data loss via an
  unpaged Xero read plus an "archive everything not returned" step). Independent
  of the others; land it directly after 022-024.
- **030 + 031** together restore the notification delivery layer, which is
  non-functional in production today (email queue has no consumer; the SSE broker
  is in-process only). 030 is additive and low-risk; 031 is the riskiest plan in
  the batch (cross-tenant channel isolation is the failure mode) and has a
  transport-decision gate.
- **028 and 029** both touch `feeds` (publication/projection) but different
  lines, so they can land in either order. Both interact with 023: once the
  mutable timestamp is out of the cache key, feed freshness rides on the
  explicit invalidation paths that 028/029 changes flow through.
- **032** now also fixes the withdraw path's notification transaction boundary
  (same defect, same file as submit). It should precede **036** so the widened
  withdraw path inherits the corrected boundary.
- **033** builds on already-landed plan 019; it is a hardening spike, lowest
  priority.
- **035** depends on **034** only for adding the export button to the out-of-office
  screen; the leave-reports export stands alone.

### Findings audited 2026-07-12 but NOT planned (lower leverage; available on request)

- Analytics service duplication: `leave-reports-service.ts` and
  `out-of-office-service.ts` share near-identical dataset loaders that have already
  drifted (holiday inclusion). Extract a shared `loadAnalyticsDataset`. Effort M.
- Clerk-role → domain-role mapping re-implemented in ~20+ page/action sites
  (`org:owner`/`org:admin`/`org:manager` ternaries); centralise in `@repo/auth`.
- Acting-person `findFirst` lookup duplicated across ~10 files; extract
  `resolveActingPersonId`.
- Client components import Prisma generated types directly
  (`@repo/database/generated/*`) in `settings/integrations/**` and
  `availability-status.ts`, breaching the data-access boundary; `turbo boundaries`
  has no tags configured to catch it.
- Dashboard fan-out (`getManagerView`/`getAdminView`) re-scans the `people` table
  ~5x and re-reads `getSettings` 6+ times per render; also `listForApprover` is
  unbounded and called org-wide for dashboard cards. Effort M each; consolidate
  loads behind the existing `DashboardCache`.
- Logs lack correlation/request IDs across the app/api/jobs boundary
  (`packages/observability/log.ts`).
- Large Xero write-back handlers (`reconcile-xero-approval-state.ts` 796 lines,
  `sync-xero-people.ts` 605 lines) have only integration coverage;
  `alternative-contact-service.ts` (599 lines) has no test at all.

### Findings considered and rejected 2026-07-12

- "Report-only CSP on `apps/app` is a gap": it is **by design** — plan 004 was
  explicitly titled "report-only CSP on apps/app", a deliberate staged rollout.
  Promoting to enforcing + removing `unsafe-inline`/`unsafe-eval` is a future
  hardening step, not a defect. Not planned.
- `bun audit` at `123bbd8`: 4 moderate/low advisories, all off the reachable
  production runtime path (build/dev/transitive tooling). Nothing to action per
  the critical/high-on-reachable-runtime bar.
- Core stack version lag: none material (Next 16.2, React 19.2, Prisma 7.8, Zod 4,
  TS 6, Clerk 7). No migration plan warranted.

## Dependency notes

The original plans landed sequentially on `preview`; all ordering dependencies
below are now satisfied or retired. These notes are historical context.

- 001 and 005 have no functional dependency on each other: both add files
  under `apps/api/__tests__/` but touch different test files.
- 004 changes `headers()` config; 005's route tests assert only route-set
  headers, so they do not conflict.
- 006 and 008 are completed doc-only spikes from the 2026-07-02 `next` audit.
  Plan 006 led to completed plan 020. Plan 008 remains a design record only.
- Plan 007 is rejected. Plan 003 remains the correct unsupported-region
  behaviour while plan 016 keeps launch scope AU-only.
- 010 is independent of 009. 010 creates an initial token for the default feed;
  009 concerns notification dispatch when a token is later rotated.
- 011 is sequenced after 010 because both plans touch the same Organisation
  creation hooks (`ensureOrganisationForClerk` and the direct Xero creation
  path). 011 does not require the default feed helper, but executing 010 first
  keeps the edits to those shared hooks in one clean sequence on `preview`.
- 012 is independent but security-sensitive; land it before exposing manual
  availability mutation routes to broader users.
- 013 should land before treating `bun run test:integration` as a reliable
  verification gate. 014 can patch dependencies without 013, but its full
  verification depends on 013 if integration tests are still schema-blocked.
- 021 removed the unrelated fixed-ID collision that blocked 013's full
  integration gate. Both plans are DONE, and 013's remaining gates passed after
  021 landed on `preview`.
- 015 is a narrow calendar authorisation fix and can land independently.
- 016 depends on 003 for the backend unsupported-region error, but covers the
  separate setup, OAuth, and marketing surfaces that currently overstate NZ/UK
  support.

## Accepted findings already covered by existing plans

- Clerk auth webhook logs the full verified payload including PII:
  `plans/002-clerk-webhook-pii-logging.md`.
- NZ/UK write-back stubs return misleading `unknown_error` codes:
  `plans/003-region-not-supported-error.md`.
- NZ payroll support remains deferred by the AU-only launch decision. The
  rejected plan 007 and its report are historical inputs only; a future NZ
  initiative needs a newly scoped full-region spike.

## Audited but not planned (accepted findings awaiting a decision)

- Feed cache invalidation uses a Redis SCAN loop over the KV REST API
  (`packages/feeds/src/cache/feed-cache.ts:87-100`). Plan 023 (2026-07-12 batch)
  removes the mutable `feedUpdatedAt` component from the cache key; the
  SCAN-loop-to-generation-key redesign itself remains an open, unplanned item
  and must preserve the `feed:${feedId}:*` prefix contract if ever done.
- Leave-balance sync upserts one row at a time
  (`packages/jobs/src/handlers/sync-xero-leave-balances.ts:265-339`); any batch
  redesign must preserve the documented record-level failure isolation rule.
- CI has no dependency caching and runs fully sequentially
  (`.github/workflows/ci.yml`). **Reconcile 2026-07-14 adds a more serious
  problem in the same file**: CI does not run on `preview` at all (triggers are
  `main`-only), so the entire 022-032 batch merged without CI. See
  "Structural finding" above.
- `sync-xero-leave-records.ts` (1008 lines) and `sync-xero-people.ts`
  (605 lines) both suppress `noExcessiveCognitiveComplexity`; refactor is L
  effort with MED risk.
- Docs still say "Vercel KV", a discontinued product; the code speaks generic
  Upstash-style Redis REST so it works, but CLAUDE.md/PRODUCT.md and env-var
  naming will mislead new deployments.
- Investigate: whether org-spanning public holiday changes
  (`organisation_id = null`) invalidate all affected feeds; unverified.
- ~~Notification emails are enqueued in `notification_email_queue` with no worker
  consuming the queue.~~ **RESOLVED** by plan 030 (`ab1be48`, verified at
  `dabb529`): `sendNotificationEmailsFunction` is registered in
  `packages/jobs/src/functions.ts` and drains the queue via Resend. The plan 008
  manager digest is therefore unblocked.
- Public pricing catalogue copy uses "Basic", while the pricing page metadata
  still says "Starter". This is a small copy follow-up, not a reason to reopen
  completed plan 020.

## Direction findings audited but not planned (2026-07-02 `next` audit)

- Out-of-office analytics screen (ScreenCatalogue S-16, build-order step 15):
  `apps/app/app/(authenticated)/analytics/` contains only `leave-reports`;
  the specified `/analytics/out-of-office` route does not exist. Effort M.
  **Now planned**: plan 034 (2026-07-12 batch).
- CSV export for leave reports: charts render but no download path exists
  under `analytics/`; exports must respect privacy transforms. Effort S-M.
  **Now planned**: plan 035 (2026-07-12 batch).
- Plan-limit pre-breach warning: billing page shows usage bars, but no
  ambient "you're at X% capacity" banner near action points (people list).
  Effort S. Still unplanned.

## Direction findings considered and rejected (2026-07-02 `next` audit)

- "Audit log UI missing": it exists at
  `apps/app/app/(authenticated)/settings/audit-log/` (page, client, actions).
- "Feed last-used telemetry invisible": `lastUsedAt` is shown in both
  `components/feed/feed-table.tsx:172-173` and `feed-detail.tsx`.
- "Plans vs availability route duplication": `/availability/page.tsx` is a
  legacy redirect to `/plans`; its `new`/`[recordId]` subroutes are the live
  manual-entry forms, not duplicates.
- "Holidays vs public-holidays route duplication": ScreenCatalogue specifies
  both (S-11 read-only view, S-23 admin settings); by design.
- "`/settings/getting-started` uncatalogued": catalogue bookkeeping, not a
  direction finding.
- "Stripe billing unconfigured / no webhook models": wrong in its particulars;
  the Stripe webhook and `stripe_events` table exist and the in-app upgrade
  flow is live (owner-gated). The real gap is the marketing pricing surface,
  which became plan 006.

## Xero authentication: state at 2026-07-04 (scoped investigation, not a full audit)

Requirement investigated: "admin users authenticate Xero; the connection is
then available to all users in the organisation." Finding: the flow is already
substantially built, and only the two gaps below (plans 017, 018) needed work.

- **"Available to all users" is already satisfied by architecture.** Xero tokens
  live on `XeroConnection` (one per `Organisation`, scoped by `clerk_org_id`),
  consumed server-side by sync jobs, feeds, and availability reads for every
  user. There are no per-user Xero tokens. No plan required.
- **Admin gating already exists on every app-layer surface**: the settings page
  and connect page use `requirePageRole("org:admin")`
  (`apps/app/.../settings/integrations/xero/page.tsx:13`,
  `.../xero/connect/page.tsx:21`); all server actions resolve `org:admin` or
  `org:owner` (`.../xero/_actions.ts:234-241`,
  `.../xero/connect/_actions.ts:34-40`). The one uncovered entry point was the
  start API route → plan 017.
- **Token lifecycle already handled**: proactive refresh with an advisory lock
  (`ensureFreshXeroConnection`), HMAC-signed OAuth state, AES-GCM token
  encryption at rest, stale/disconnected status transitions. No plan required.
- **Gap 1 (plan 017)**: the OAuth start API route is not role-checked.
- **Gap 2 (plan 018)**: disconnect does not revoke at Xero
  (`DELETE /connections/{id}`); the connection id is discarded at OAuth time.
  This is the subject of the Xero "managing connections" best-practices doc.

Not re-audited here: Xero read/write region adapters, sync-job internals, the
matches (person-linking) flow — out of scope for the authentication question.

## Xero token refresh: state at 2026-07-04 (scoped investigation, not a full audit)

Requirement investigated: the Xero OAuth2 token refresh strategy (30-minute
access token; single-use rotating refresh token valid 60 days). Finding: the
proactive refresh core is sound (`ensureFreshXeroConnection` refreshes within a
5-minute buffer under a per-connection `pg_advisory_xact_lock`; all sync jobs and
the write adapter go through it), but three coupled gaps needed hardening — all
folded into plan 019:

- **Refresh errors are unclassified** (`exchangeToken` returns `unknown_error`
  for everything), so callers cannot distinguish a transient 5xx from a dead
  refresh token (`invalid_grant`).
- **`markXeroConnectionStale` has no callers** — a dead refresh token never
  transitions the connection to `stale`, so scheduled syncs loop on a doomed
  refresh and the reconnect UI never appears.
- **The manual "Refresh" action bypasses the advisory lock**
  (`refreshXeroOAuthConnection`), racing scheduled refreshes and risking a
  bricked connection under single-use token rotation.

Audited but **not planned** (deferred decision):

- **60-day idle keep-alive.** Tokens stay alive only because scheduled syncs
  refresh them. A connection with `sync_paused_at` set, or an org idle for 60
  days, will let the refresh token lapse and require a full reconnect. There is
  no keep-alive job. Whether to build one (a scheduled Inngest job that refreshes
  idle-but-active connections before the 60-day boundary) depends on whether
  long-term paused connections are a supported product state. Becomes a plan only
  if the maintainer confirms that need.

## Findings considered and rejected

- ".env.local committed with live secrets": false. The file is untracked and
  gitignored (`git ls-files` shows only `.env.example` files; no history).
- "Stripe webhook should return 401 not 400 on bad signature / 400 drops
  events": Stripe retries on any non-2xx; 400 is standard and loses nothing.
- "recharts is an unused dependency": it powers
  `packages/design-system/components/ui/chart.tsx` and the leave-reports chart.
- "Feed rebuild enqueueing is O(N*M) per-record queries": misread; it is two
  batched queries plus one batched Inngest send
  (`sync-xero-leave-records.ts:712-763`).
- "Hardcoded org id breaks usage recounts and plan enforcement": the
  `recount-usage` handler only uses `clerkOrgId`, which the webhook passes
  correctly; the placeholder `organisationId` is cosmetic (folded into plan 001).
- Six "fragile but currently correct" security patterns (timing oracles on
  token hashes, organisationId-in-body with validation present, decrypt error
  wrapping, comment-only suggestions): current code validates correctly; not
  worth dedicated plans.

## Not audited

`apps/web` marketing internals beyond the region-support copy surfaces covered
by plan 016, `apps/docs` content, `packages/design-system` primitives,
`tooling/`.
