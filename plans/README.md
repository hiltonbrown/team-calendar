# Implementation Plans

Generated by the improve skill (deep audit) on 2026-06-12, against commit `d9da765`. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honour its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Issue | Status |
|------|-------|----------|--------|------------|-------|--------|
| 001 | Add lint and typecheck gates to CI | P1 | S | — | [#57](https://github.com/hiltonbrown/leavesync/issues/57) | DONE |
| 002 | Serve 304 from the ICS feed endpoint; decouple cache write | P1 | S | 001 (soft) | [#58](https://github.com/hiltonbrown/leavesync/issues/58) | DONE |
| 003 | Fail-closed Xero token decryption | P1 | S | — | [#59](https://github.com/hiltonbrown/leavesync/issues/59) | DONE |
| 004 | Restrict SSE CORS to the app origin; guard keep-alive | P1 | S | — | [#60](https://github.com/hiltonbrown/leavesync/issues/60) | DONE |
| 005 | Pin "latest" dependency versions; raise Node engines floor | P1 | S | — | [#61](https://github.com/hiltonbrown/leavesync/issues/61) | DONE |
| 006 | Batch per-feed people/teams queries in the feed list | P2 | M | — | [#62](https://github.com/hiltonbrown/leavesync/issues/62) | DONE |
| 007 | Stop listPeople fetching the whole organisation | P2 | M | — | [#63](https://github.com/hiltonbrown/leavesync/issues/63) | DONE |
| 008 | Serialise concurrent Xero token refreshes | P2 | M | — | [#64](https://github.com/hiltonbrown/leavesync/issues/64) | DONE |
| 009 | Characterisation tests for reconcile-xero-approval-state | P2 | L | — | [#65](https://github.com/hiltonbrown/leavesync/issues/65) | DONE |
| 010 | Test coverage for the feed token service | P2 | M | — | [#66](https://github.com/hiltonbrown/leavesync/issues/66) | DONE (unit/package/check verified; DB-backed integration path skipped locally because DATABASE_URL is absent) |
| 011 | Move implementation code out of the availability barrel | P3 | M | 001 | [#67](https://github.com/hiltonbrown/leavesync/issues/67) | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 001 first: it establishes the lint/typecheck gates every other plan uses as verification commands. 002 and 011 reference `bun run typecheck`, which 001 creates; without 001, substitute per-app `cd apps/<app> && bun run typecheck`.
- 010 (token-service tests) is the safety net for 002 (both touch the feed token/render contract) — landing 010 before or alongside 002 is preferable but not required.
- 009 (reconcile tests) is the safety net for 003 and 008 (both change `packages/xero` behaviour the job depends on).
- 006 and 007 are independent of everything and of each other.
- 011 touches the repo's highest-churn file — execute it in a quiet window and merge promptly.

## Findings considered and rejected

- **Committed secrets in `.env.local`**: false — `git ls-files` and full history show no `.env*` file was ever tracked; `.gitignore` covers all variants. Local-only files are normal. No rotation needed.
- **Manual availability types missing from the UI**: false — `apps/app/app/(authenticated)/plans/record-form.tsx` exposes the full type selector (wfh, travelling, training, client_site, …).
- **CSRF on server actions**: Next.js server actions are same-origin-protected by design; not a finding.
- **Feed token hint leaks structure**: tokens are 240-bit random; the last-4-chars hint is an accepted UX trade-off. Not worth doing.
- **Barrel files for `apps/app/lib`**: contradicts the repo's "no barrel files" rule. Rejected.
- **Incremental Xero sync (`ModifiedAfter`)**: real finding (every sync re-fetches everything) but L-effort and needs per-endpoint API investigation — deliberately not planned this round; revisit if Xero rate limits start binding.
- **`markTokenUsed` write-per-poll on cache hits**: noted in plan 002's maintenance notes as a deferred follow-up.
- **`@react-email` 5.x/6.x version spread**: real but contained to the email dev tooling; deferred (noted in plan 005).

## Direction findings (unplanned — maintainer decisions)

Recorded from the audit for future consideration; none were selected for planning this round:

1. NZ/UK Xero write-back is stubbed (`packages/xero/src/{nz,uk}/write.ts`) while README/PRODUCT.md claim three-region support — either implement or scope claims to AU.
2. Analytics services (`packages/availability/src/analytics/`) are complete and tested but have no UI routes — ~a day of UI work unlocks two finished features (ScreenCatalogue S-15/S-16).
3. Billing computes `isOverLimit` but no creation path enforces it — measurement without enforcement.
4. Bulk approval/decline operations don't exist — single-record flow only.
