# Implementation Plans

Generated by the improve skill on 2026-07-02 against commit `8790bdb`. Execute
in the order below unless dependencies say otherwise. Each executor: read the
plan fully before starting, honour its STOP conditions, and update your row
when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Stripe webhook: fail loudly, drop placeholder org id, add tests | P1 | M | none | TODO |
| 002 | Stop logging full Clerk webhook body (PII) | P1 | S | none | TODO |
| 003 | region_not_supported_error for NZ/UK write-back stubs | P2 | S | none | TODO |
| 004 | Security response headers + report-only CSP on apps/app | P2 | M | none | TODO |
| 005 | Tests for ICS feed route and availability routes | P2 | M | none | TODO |
| 006 | Pricing surface decision spike (doc-only) | P1 | S | none | TODO |
| 007 | NZ payroll write-back spike (doc-only) | P2 | M | none | TODO |
| 008 | Manager weekly digest design spike (doc-only) | P3 | S | none | TODO |
| 009 | Dispatch feed_token_rotated on rotation | P3 | S-M | none | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- No hard dependencies. 001 and 005 both add files under `apps/api/__tests__/`
  but touch different test files; they can land in either order.
- 004 changes `headers()` config; 005's route tests assert only route-set
  headers, so they do not conflict.
- 006, 007, 008 are doc-only spikes (direction findings from the 2026-07-02
  `next` audit); each produces a report in `plans/` and a follow-up
  implementation plan is written only after the maintainer decides.
- 008 and 009 both need the registry `emailTemplate` string-to-template
  mapping traced; whichever executes first should record the answer for the
  other.
- Plan 003 (region error) is the stopgap for the gap plan 007 investigates;
  003 can land any time, 007's build supersedes it for NZ later.

## Audited but not planned (accepted findings awaiting a decision)

- Feed cache invalidation uses a Redis SCAN loop over the KV REST API
  (`packages/feeds/src/cache/feed-cache.ts:87-100`); cache keys already embed
  `feedUpdatedAt`, so a generation-style key would make invalidation cheap.
- Leave-balance sync upserts one row at a time
  (`packages/jobs/src/handlers/sync-xero-leave-balances.ts:265-339`); any batch
  redesign must preserve the documented record-level failure isolation rule.
- CI has no dependency caching and runs fully sequentially
  (`.github/workflows/ci.yml`).
- `sync-xero-leave-records.ts` (1008 lines) and `sync-xero-people.ts`
  (605 lines) both suppress `noExcessiveCognitiveComplexity`; refactor is L
  effort with MED risk.
- Docs still say "Vercel KV", a discontinued product; the code speaks generic
  Upstash-style Redis REST so it works, but CLAUDE.md/PRODUCT.md and env-var
  naming will mislead new deployments.
- Investigate: whether org-spanning public holiday changes
  (`organisation_id = null`) invalidate all affected feeds; unverified.

## Direction findings audited but not planned (2026-07-02 `next` audit)

- Out-of-office analytics screen (ScreenCatalogue S-16, build-order step 15):
  `apps/app/app/(authenticated)/analytics/` contains only `leave-reports`;
  the specified `/analytics/out-of-office` route does not exist. Effort M.
- CSV export for leave reports: charts render but no download path exists
  under `analytics/`; exports must respect privacy transforms. Effort S-M.
- Plan-limit pre-breach warning: billing page shows usage bars, but no
  ambient "you're at X% capacity" banner near action points (people list).
  Effort S.

## Direction findings considered and rejected (2026-07-02 `next` audit)

- "Audit log UI missing": it exists at
  `apps/app/app/(authenticated)/settings/audit-log/` (page, client, actions).
- "Feed last-used telemetry invisible": `lastUsedAt` is shown in both
  `components/feed/feed-table.tsx:172-173` and `feed-detail.tsx`.
- "Plans vs availability route duplication": `/availability/page.tsx` is a
  legacy redirect to `/plans`; its `new`/`[recordId]` subroutes are the live
  manual-entry forms, not duplicates.
- "Holidays vs public-holidays route duplication": ScreenCatalogue specifies
  both (S-11 read-only view, S-23 admin settings); by design.
- "`/settings/getting-started` uncatalogued": catalogue bookkeeping, not a
  direction finding.
- "Stripe billing unconfigured / no webhook models": wrong in its particulars;
  the Stripe webhook and `stripe_events` table exist and the in-app upgrade
  flow is live (owner-gated). The real gap is the marketing pricing surface,
  which became plan 006.

## Findings considered and rejected

- ".env.local committed with live secrets": false. The file is untracked and
  gitignored (`git ls-files` shows only `.env.example` files; no history).
- "Stripe webhook should return 401 not 400 on bad signature / 400 drops
  events": Stripe retries on any non-2xx; 400 is standard and loses nothing.
- "recharts is an unused dependency": it powers
  `packages/design-system/components/ui/chart.tsx` and the leave-reports chart.
- "Feed rebuild enqueueing is O(N*M) per-record queries": misread; it is two
  batched queries plus one batched Inngest send
  (`sync-xero-leave-records.ts:712-763`).
- "Hardcoded org id breaks usage recounts and plan enforcement": the
  `recount-usage` handler only uses `clerkOrgId`, which the webhook passes
  correctly; the placeholder `organisationId` is cosmetic (folded into plan 001).
- Six "fragile but currently correct" security patterns (timing oracles on
  token hashes, organisationId-in-body with validation present, decrypt error
  wrapping, comment-only suggestions): current code validates correctly; not
  worth dedicated plans.

## Not audited

`apps/web` marketing internals, `apps/docs` content, `packages/design-system`
primitives, `tooling/`.
