# Xero provider contract ledger

Read date for all sources: 2026-09-22. The ledger records the HTTP calls made by
the current Xero adapter. A row marked `OBSERVED` describes behaviour verified in
the repository call site; `DOCUMENTED` describes a contract stated by the linked
primary Xero documentation. Where the source does not establish an endpoint
detail, the row remains `NOT VERIFIED`.

| Method/path | Token class | Minimum documented scope | Tenant-header requirement | Pagination/completeness | Idempotency/uncertain outcome | Deadline class | Rate bucket | Source URL | Date read | Verification status |
|---|---|---|---|---|---|---|---|---|---|---|
| POST `https://identity.xero.com/connect/token`, `grant_type=authorization_code` | User OAuth access and refresh token response | `offline_access` for refresh-token response; requested payroll scopes are app-configured | Not applicable to token endpoint | Single code exchange; code is single-use and expires after 5 minutes | Code exchange is not idempotent; retry after an accepted response is uncertain | No explicit timeout in current caller | Bootstrap org bucket, keyed by Clerk org and optional organisation | https://developer.xero.com/documentation/guides/oauth2/auth-flow | 2026-09-22 | DOCUMENTED |
| POST `https://identity.xero.com/connect/token`, `grant_type=refresh_token` | User refresh token, returns rotated access and refresh tokens | `offline_access` | Not applicable to token endpoint | Single token response | Refresh-token rotation is not idempotent; Xero documents a 30-minute retry grace period when the response or persistence is lost | No explicit timeout in current caller | Bootstrap org bucket, keyed by Clerk org and optional organisation | https://developer.xero.com/documentation/guides/oauth2/token-types | 2026-09-22 | DOCUMENTED |
| GET `https://api.xero.com/connections` | User access token or app-management access token | User inventory uses the connected user token; app-management form uses singular `scope=app.connections` | Not required by the endpoint | JSON array; source documents optional `authEventId`, but current call sends no filter and no pagination parameter | Read-only inventory; current parser ignores malformed entries rather than retrying them | No explicit timeout in current caller | Bootstrap org bucket, keyed by Clerk org and optional organisation | https://developer.xero.com/documentation/best-practices/managing-connections/connections | 2026-09-22 | DOCUMENTED |
| DELETE `https://api.xero.com/connections/{connectionId}` | User access token or app-management access token | User cleanup uses the connected user token; app-management form uses singular `scope=app.connections` | Not required by the endpoint | Targeted single connection; no pagination | Targeted delete is remote-state changing and response loss is uncertain; current code treats 404 as already absent and otherwise allows one attempt | No explicit timeout; current caller sets one attempt | Bootstrap org bucket, keyed by Clerk org and optional organisation | https://developer.xero.com/documentation/best-practices/managing-connections/connections | 2026-09-22 | DOCUMENTED |
| POST `https://identity.xero.com/connect/token`, `grant_type=client_credentials`, management token | App management access token | Singular form parameter `scope=app.connections` | Not applicable to token endpoint | Single token response with `access_token`, `expires_in`, and `token_type`; no refresh token | Token acquisition is not idempotent in the sense of preserving one token; the returned access token can call GET `/connections` and targeted DELETE `/connections/{connectionId}` | No explicit timeout in current implementation because this path is not implemented | App-management bucket is not implemented; do not infer a production bucket | https://developer.xero.com/documentation/guides/oauth2/client-credentials/ | 2026-09-22 | DOCUMENTED |
| GET `https://api.xero.com/api.xro/2.0/Organisation` | User access token | Exact minimum payroll scope is not stated on the source page; current app requests payroll scopes | Required: `Xero-Tenant-Id` | One organisation response; no pagination | Read-only region discovery; current parser treats a missing or unsupported country as an error | No explicit timeout in current caller | Per-organisation bucket: 60/min, 5,000/day, 5 concurrent, plus 10,000/min app-wide | https://developer.xero.com/documentation/guides/oauth2/token-types | 2026-09-22 | OBSERVED |
| GET `https://api.xero.com/payroll.xro/1.0/Employees?page={page}` | User access token | `payroll.employees.read` is requested by the app; endpoint-specific minimum is not independently confirmed here | Required: `Xero-Tenant-Id` | Current code requests pages of 100, stops on raw page length below 100, and marks incomplete after 200 pages or an envelope parse failure | Read-only; retry policy is shared reactive retry for transient failures | No explicit timeout in current caller | Per-organisation bucket: 60/min, 5,000/day, 5 concurrent, plus 10,000/min app-wide | https://developer.xero.com/documentation/api/payrollau/employees | 2026-09-22 | DOCUMENTED |
| GET `https://api.xero.com/payroll.xro/1.0/Employees/{employeeId}` | User access token | `payroll.employees.read` is requested by the app; endpoint-specific minimum is not independently confirmed here | Required: `Xero-Tenant-Id` | Single employee response | Read-only; current balance loop uses one attempt and isolates ordinary employee errors | No explicit timeout in current caller | Per-organisation bucket: 60/min, 5,000/day, 5 concurrent, plus 10,000/min app-wide | https://developer.xero.com/documentation/api/payrollau/employees | 2026-09-22 | DOCUMENTED |
| GET `https://api.xero.com/payroll.xro/1.0/LeaveApplications/v2?page={page}` | User access token | Current app requests `payroll.payruns.read`; endpoint-specific minimum is NOT VERIFIED | Required: `Xero-Tenant-Id` | Current code requests pages of 100 and stops on mapped record count below 100; max 200 pages; completeness is therefore an adapter rule, not a provider guarantee | Read-only; shared transient retry policy | No explicit timeout in current caller | Per-organisation bucket: 60/min, 5,000/day, 5 concurrent, plus 10,000/min app-wide | https://developer.xero.com/documentation/api/payrollau/release-notes | 2026-09-22 | NOT VERIFIED |
| GET `https://api.xero.com/payroll.xro/1.0/LeaveApplications/{leaveApplicationId}` | User access token | Current app requests `payroll.payruns.read`; endpoint-specific minimum is NOT VERIFIED | Required: `Xero-Tenant-Id` | Single leave application response | Read-only; shared transient retry policy | No explicit timeout in current caller | Per-organisation bucket: 60/min, 5,000/day, 5 concurrent, plus 10,000/min app-wide | https://developer.xero.com/documentation/api/payrollau/release-notes | 2026-09-22 | NOT VERIFIED |
| GET `https://api.xero.com/payroll.xro/1.0/PayItems` | User access token | Current app requests `payroll.settings.read`; endpoint-specific minimum is NOT VERIFIED | Required: `Xero-Tenant-Id` | One aggregate PayItems response; source says GET returns all PayItems, with no pagination contract stated | Read-only; shared transient retry policy | No explicit timeout in current caller | Per-organisation bucket: 60/min, 5,000/day, 5 concurrent, plus 10,000/min app-wide | https://developer.xero.com/documentation/api/payrollau/payitems | 2026-09-22 | DOCUMENTED |
| POST `https://api.xero.com/payroll.xro/1.0/LeaveApplications` | User access token | Current app requests `payroll.payruns`; endpoint-specific minimum is NOT VERIFIED | Required: `Xero-Tenant-Id` | Single-item write response; no provider idempotency key is supplied | Ambiguous on timeout or 5xx; current code disables ambiguous retries to avoid duplicate leave applications | 120-second request timeout | Per-organisation bucket: 60/min, 5,000/day, 5 concurrent, plus 10,000/min app-wide | https://developer.xero.com/documentation/api/payrollau/stp-changes | 2026-09-22 | NOT VERIFIED |
| POST `https://api.xero.com/payroll.xro/1.0/LeaveApplications/{leaveApplicationId}/approve` | User access token | Current app requests `payroll.payruns`; endpoint-specific minimum is NOT VERIFIED | Required: `Xero-Tenant-Id` | Single state-transition response | State transition is not proven idempotent by the source or current adapter; ambiguous retries are disabled | 120-second request timeout | Per-organisation bucket: 60/min, 5,000/day, 5 concurrent, plus 10,000/min app-wide | https://developer.xero.com/documentation/api/payrollau/release-notes | 2026-09-22 | NOT VERIFIED |
| POST `https://api.xero.com/payroll.xro/1.0/LeaveApplications/{leaveApplicationId}/reject` | User access token | Current app requests `payroll.payruns`; endpoint-specific minimum is NOT VERIFIED | Required: `Xero-Tenant-Id` | Single state-transition response; used for both decline and withdraw in current code | State transition is not proven idempotent by the source or current adapter; ambiguous retries are disabled | 120-second request timeout | Per-organisation bucket: 60/min, 5,000/day, 5 concurrent, plus 10,000/min app-wide | https://developer.xero.com/documentation/api/payrollau/release-notes | 2026-09-22 | NOT VERIFIED |

The current repository contains no client-credentials management-token call. The
management-token row records the documented route needed by later cleanup work.
Only this app's management-tier enablement and credential provisioning remain
NOT VERIFIED local configuration items before destructive cleanup is enabled.

NZ and UK read endpoints are excluded because current OAuth rejects NZ and UK
before persistence, and NZ and UK writes are stubs, so those endpoints are not
reachable through the active production connection flow.
